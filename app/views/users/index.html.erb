<% provide(:title, 'All uses') %>
<h1>All Users</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li class="clearfix">
      <%= gravatar_for(user, size:50) %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
